# MySQL database configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root

# If your MySQL root user has a password, fill it in here.
# If it has no password, leave it empty, e.g. DB_PASSWORD=
DB_PASSWORD=your_mysql_password_here

DB_NAME=event_system
DB_CHARSET=utf8mb4
